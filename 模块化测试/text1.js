import { Dog } from './test3.js';
import { Bane } from './text2.js';

export class Dish {
    constructor() {
        this.bine;
    }

    static Bene({ name, age }) {
        return new Bane({ name, age })
    }
    static dog({ name, age, color } = {}) {
        return new Dog({ name, age, color })
    }

}
Dish["colores"] = "1"